#include "knight.h"

Knight::Knight(char type): Piece{type} {}

bool Knight::is_valied_move(std::vector<std::vector<std::shared_ptr<Piece>>> board,
             Position from,Position to) {
        return true;
}
