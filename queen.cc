#include "queen.h"

Queen::Queen(char type): Piece{type} {}

bool Queen::is_valied_move(std::vector<std::vector<std::shared_ptr<Piece>>> board,
             Position from,Position to) {
        return true;
}
