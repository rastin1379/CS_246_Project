#include "rook.h"

Rook::Rook(char type): Piece{type} {}

bool Rook::is_valied_move(std::vector<std::vector<std::shared_ptr<Piece>>> board,
             Position from,Position to) {
        return true;
}
