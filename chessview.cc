#include "chessview.h"
#include "textobserver.h"

using namespace std;

ChessView::run() {
	string command;
	shared_ptr<Board> board = make_shared<Board>();
	observers.push_back(make_shared<TextObserver>( // to complte ))
		));
	while (true) {
	
	}	
}
